/**
 * Porcelain Design System — Shared typed class strings
 *
 * Use these exported constants in any component file so your
 * utility classes are declared in one place and stay consistent.
 */

// ─── Cards ───────────────────────────────────────────────────────────────────
/** Default surface card */
export const cardClass =
    'bg-surface border border-border rounded-2xl';

/** Elevated / action card */
export const cardElevatedClass =
    'bg-surface border border-border rounded-2xl shadow-sm';

// ─── Buttons ─────────────────────────────────────────────────────────────────
/** Primary CTA button pill */
export const btnPrimaryClass =
    'rounded-full bg-primary py-4 px-8 items-center justify-center';

/** Accent / destructive button pill */
export const btnAccentClass =
    'rounded-full bg-accent py-4 px-8 items-center justify-center';

/** Outline button pill */
export const btnOutlineClass =
    'rounded-full border border-primary py-4 px-8 items-center justify-center';

// ─── Typography ──────────────────────────────────────────────────────────────
export const textHeading1 = 'font-inter text-3xl font-bold text-text tracking-tight';
export const textHeading2 = 'font-inter text-2xl font-bold text-text tracking-tight';
export const textHeading3 = 'font-inter text-xl font-semibold text-text';
export const textBody = 'font-inter text-base text-text leading-6';
export const textCaption = 'font-inter text-sm text-text/60';
export const textLabel = 'font-inter text-sm font-medium text-text';
export const textBtnPrimary = 'font-inter text-base font-semibold text-white';

// ─── Layout helpers ──────────────────────────────────────────────────────────
/** Full-screen page background */
export const pageClass = 'flex-1 bg-background';

/** Standard page padding (4px grid × 6 = 24 px) */
export const pagePadClass = 'px-6 py-6';

/** Section separator */
export const dividerClass = 'border-b border-border my-4';
